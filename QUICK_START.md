# خطوات التشغيل السريعة

## 1. احصل على مفتاح Groq API (مجاني)
1. اذهب إلى: https://console.groq.com
2. سجل دخول أو أنشئ حساب
3. اذهب إلى "API Keys"
4. أنشئ مفتاح جديد وانسخه

## 2. أضف المفتاح في Backend
1. افتح الملف: `backend/.env`
2. استبدل `GROQ_API_KEY=` بمفتاحك:
   ```
   GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxx
   PORT=5000
   ```

## 3. شغّل المشروع

### افتح Terminal أولى (Backend):
```bash
cd backend
npm run dev
```

يجب أن ترى: `✅ Server is running on http://localhost:5000`

### افتح Terminal ثانية (Frontend):
```bash
cd frontend
npm run dev
```

يجب أن ترى رابط مثل: `http://localhost:5173`

## 4. استخدم التطبيق
1. افتح المتصفح على `http://localhost:5173`
2. الصق رابط فيديو YouTube
3. اضغط "استخراج النص"
4. اختر نوع المعالجة المطلوبة
5. شاهد النتيجة!

## مشاكل شائعة؟

### "Module not found"
```bash
# في مجلد backend
cd backend
npm install

# في مجلد frontend
cd frontend
npm install
```

### "فشل الاتصال بالخادم"
تأكد من تشغيل Backend أولاً (الخطوة 3 أعلاه)

### "خطأ في Groq API"
تأكد من وضع مفتاح API صحيح في `backend/.env`

---

🎉 **استمتع بالاستخدام!**
