{
  "name": "youtube-transcript-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "@vercel/node": "^5.6.3",
    "groq-sdk": "^0.5.0",
    "youtube-transcript": "^1.0.4"
  }
}
